import { createSlice } from "@reduxjs/toolkit";


const initValues : {
    username: string,
    password: string
}
 = {
    username: "",
    password: ""
 }
export const displayUserSlice = createSlice({
    name: 'display-user',
    ini
})